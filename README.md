### Using Python flask for backend and HTML, bootstrap, JS for the frontend

### Requirements

` pip install flask` to start the server and then
` run app.py` 
`load userdata.html` try to search email and will see the user info
